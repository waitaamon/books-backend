import Vue from 'vue'

export const Homepage = Vue.component('home-page', require('./Homepage.vue').default)
export const WebBook = Vue.component('web-book', require('./WebBook.vue').default)
export const WebChapter = Vue.component('web-chapter', require('./WebChapter.vue').default)
